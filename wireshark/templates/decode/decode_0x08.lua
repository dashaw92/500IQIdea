-- Update Health: Server to Client only. Sent by the server to update/set the health of the player. Added in protocol version 5. Food saturation acts as a food 'overcharge'. Food values will not decrease while the saturation is over zero.
function decode_0x08(tree, buffer, length)  
-- TODO
end

